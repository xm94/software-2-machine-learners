exports.computeProgress = function computeProgress(){
    //for all subtasks
    //if all done -> done
    //if some done or in progress -> in progress
    //if none started -> not started
}

//task insert
//if has children -> computeProgress
//else progress: object.progress